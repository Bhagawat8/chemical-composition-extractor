# Core dependencies
torch>=2.0.0
transformers==4.46.3
tokenizers==0.20.3

# PDF and Image processing
pdf2image>=1.16.0
Pillow>=9.0.0
pytesseract>=0.3.10

# Data handling
pandas>=1.5.0
numpy>=1.24.0

# DeepSeek model dependencies
einops
addict
easydict
safetensors
accelerate
tqdm

# Optional: Flash Attention (for GPU acceleration)
# flash-attn>=2.7.0
